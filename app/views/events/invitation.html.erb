<!DOCTYPE html>
<html>
  <head>
    <title>Undangapp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <script src="https://kit.fontawesome.com/ac49b349b5.js" crossorigin="anonymous"></script>
  </head>

  <body>
    <div class="container-md">
      <div class="mx-auto" style="max-width: 400px">
        <div class="vh-100 d-flex align-items-center border border-dark border-5 rounded">
          <div class="text-center mx-auto">
            <h1 class="h1 fw-bold">UNDANGAN</h1>
            <p class="fs-5">untuk</p>
            <h2 class="h2 fw-bold mb-5"><%= @recipient.name %></h2>
            <a class="link-dark" href="#content"><i class="fa-solid fa-down-long fs-1"></i></a>
          </div>
        </div>
        <div class="vh-100 d-flex align-items-center border border-dark border-5 rounded" id="content">
          <div class="mx-auto p-3 fs-5">
            <% if @event.opening %>
              <p class="mb-4"><%= @event.opening %></p>
            <% end %>
            <h2 class="fw-bold mb-0"><%= @event.name %></h2>
            <p class="fw-bold mb-0"><%= @event.date.strftime("#{@day_id[@event.date.strftime("%w").to_i]}, %d #{@month_id[@event.date.strftime("%-m").to_i - 1]} %Y - %H:%M") %></p>
            <p class="mb-1"><%= @event.place %></p>
            <% if @event.description %>
              <p class="fst-italic mb-4"><%= @event.description %></p>
            <% end %>
            <% if @event.closing %>
              <p><%= @event.closing %></p>
            <% end %>
            <div class="w-50 float-end fw-bold">
            <% if @event.sender_status %>
              <p class=""><%= @event.sender_status %></p>
            <% end %>
            <p class="mt-5"><%= @event.sender %></p>
            </div>
          </div>
        </div>
      </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </body>
</html>

<div>
  
</div>